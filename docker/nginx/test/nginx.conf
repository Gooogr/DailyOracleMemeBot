events {}

http {
    server {
        listen 80;

        #  Bare minimum example for nginx proxy check. In test config all ports are exposed anyway
        location /minio/ {
            proxy_pass http://minio:9000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
